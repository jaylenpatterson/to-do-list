const express = require('express');
const router  = express.Router();

module.exports = (db) => {
  router.get("/", (req, res) => {

  });

  // Registration Users
  router.post('/', (req, res) => {
   
  });

  return router;
};